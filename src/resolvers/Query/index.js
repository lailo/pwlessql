const discover = require('resolvers/Query/discover')
const entity = require('resolvers/Query/entity')
const profile = require('resolvers/Query/profile')
const user = require('resolvers/Query/user')

module.exports = {
  discover,
  entity,
  profile,
  user,
}
