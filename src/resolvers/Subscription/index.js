const { addEntity } = require('resolvers/Subscription/Entity/add')
const { updateEntity } = require('resolvers/Subscription/Entity/update')
const { removeEntity } = require('resolvers/Subscription/Entity/remove')

module.exports = {
  addEntity,
  updateEntity,
  removeEntity,
}
